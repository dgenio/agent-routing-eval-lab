"""Governed-path components: bounded choices, context firewall, and action guard.

These turn the failure modes the unsafe baseline demonstrates (full-catalog
distraction, raw tool output trusted as instruction, unapproved sensitive writes)
into contained, auditable behavior. They are illustrative governance patterns for
an offline demo, not production security controls.
"""
